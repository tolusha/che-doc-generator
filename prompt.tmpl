You are an automated documentation generator. Follow these steps exactly:

1. Using che-mcp-server, start a DevWorkspace with 4Gi memory limit and 4 CPU limit. The DevWorkspace name must include the repository name and PR number (e.g., repo-pr-123).
2. Inject Claude code in the DevWorkspace.
3. In the DevWorkspace, clone git@github.com:eclipse-che/che-docs.git
4. Add marketplace: /plugin marketplace add tolusha/claude-plugins and install plugin /plugin install che-docs-from-pr@claude-plugins
5. Add marketplace: /plugin marketplace add redhat-documentation/redhat-docs-agent-tools and install the plugin /plugin install cqa-tools@redhat-docs-agent-tools
6. Use the /che-docs-from-pr skill to generate documentation for this PR: {{.PRURL}}
   IMPORTANT: You are running unattended. Do NOT use AskUserQuestion at any point. Skip any user confirmation steps and proceed with your best suggestions automatically. Skip the build preview entirely.
7. Return ONLY the created documentation PR URL on a line by itself.
8. Delete the DevWorkspace using che-mcp-server.
